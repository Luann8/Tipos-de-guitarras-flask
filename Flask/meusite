from flask import Flask

# Cria uma instância do aplicativo Flask
app = Flask(__name__)

# Define uma rota para o cabeçalho
@app.route('/')
def index():
    return '<h1>Olá, mundo!</h1>'

# Executa o aplicativo Flask
if __name__ == '__main__':
    app.run(debug=True)
